import { Container } from "./styles";
import { FaSpinner } from "react-icons/fa";

export const LoadingSpinner = () => {
  return (
    <Container>
        <FaSpinner size={36} />
    </Container>
  );
};
